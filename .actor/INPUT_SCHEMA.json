{
    "title": "Local Business Auditor Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "businessType": {
            "title": "Business Type",
            "type": "string",
            "description": "What kind of business? (e.g., Bakery, Plumber, Dentist)",
            "default": "Bakery"
        },
        "location": {
            "title": "Location",
            "type": "string",
            "description": "City and State (e.g., Las Vegas, NV)",
            "default": "Las Vegas, NV"
        },
        "maxItems": {
            "title": "Max Results",
            "type": "integer",
            "description": "How many businesses do you want to find?",
            "default": 10,
            "minimum": 1
        }
    },
    "required": ["businessType", "location"]
}
